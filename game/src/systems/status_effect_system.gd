class_name StatusEffectSystem

# Tick pipeline step 12: Process status effects (DOTs, buffs, debuffs).
# Phase 1 stub — just tick down active DOT durations. Full implementation Phase 3+.


func tick(ecs: ECS, tick_count: int) -> void:
	# TODO Phase 3: tick DOTs, buffs, debuffs
	pass
