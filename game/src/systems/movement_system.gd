class_name MovementSystem

# Processes unit movement each simulation tick.
# Phase 1 implementation — reads MoveCommand / PathState / Position / MoveSpeed,
# writes Position / Velocity.

func tick(ecs: ECS, tick_count: int) -> void:
	# TODO: Implement in Phase 1 after movement interface is finalized
	# Will read MoveCommand, PathState, Position, MoveSpeed components
	# Will write Position, Velocity
	pass
